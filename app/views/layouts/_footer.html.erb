<% if current_user && current_page?(gardener_path) %>
  <div class="row pt-2 pb-2 justify-content-center">
    <div class="col-md-auto contact_us">
      <h6 class="card-subtitle mb-2 text-muted">Welcome, <%= current_user.first_name %></h6>
    </div>
  </div>
<% end %>
